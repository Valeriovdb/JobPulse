# Copy this to .streamlit/secrets.toml (gitignored) for local dev.
# On Streamlit Cloud, add these in the app's Secrets settings.

SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_KEY = "your-service-role-key"
